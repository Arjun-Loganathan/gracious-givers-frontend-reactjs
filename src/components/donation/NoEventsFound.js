import './styles/NoEventsFound.css';

const NoEventsFound = () => {
    return (
        <div className="noEvent">
            No event(s) found.
        </div>
    );
}

export default NoEventsFound;
